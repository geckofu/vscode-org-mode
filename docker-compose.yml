services:
  vscode-org-mode-builder:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # Mount current directory to preserve the .vsix output
      - ./dist:/workspace/dist
    command: sh -c "vsce package -o dist/ --allow-star-activation --skip-license && ls -lh dist/*.vsix"
